import reviewController from '../controllers/reviewController.js';	
import { Router } from 'express';

import { isLoggedIn } from '../utils/isAuthenticated.js';

const router = Router();



export default router;